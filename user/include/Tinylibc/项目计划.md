# 一阶段计划
完成文件I/O操作函数，并且编写用户程序验证

这些函数用于文件的打开、关闭、读写和定位等基本操作。
函数名	功能描述
open	打开（或创建）一个文件或设备，返回文件描述符

close	关闭一个文件描述符，释放相关资源

read	从文件描述符对应的文件中读取数据到缓冲区

write	将缓冲区中的数据写入到文件描述符对应的文件中

lseek	重新定位文件的读写偏移量

fsync	将文件描述符对应的所有修改过的缓冲区数据同步到物理介质

ftruncate	将文件描述符对应的文件截断或扩展至指定长度

unlink	删除一个文件（目录项）
